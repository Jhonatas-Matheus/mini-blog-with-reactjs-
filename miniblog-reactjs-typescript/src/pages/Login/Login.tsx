import React from "react";
import Input from "../../components/Input/Input";

type Props = {};

const Login = (props: Props) => {
  return (
    <div>
      <Input title="radius" type="text"></Input>
    </div>
  );
};

export default Login;
